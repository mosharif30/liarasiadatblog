name: CD-Liara
on:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: "16"
      - name: update-liara
        env:
          LIARA_TOKEN: ${{ secrets.LIARA_API_TOKEN }}
        run: |
          npm i -g @liara/cli@2
          liara deploy --app="amirsiadat" --api-token="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySUQiOiI2MzEwOGE1M2UwMWYzMjEzNGFlNmIwYjIiLCJpYXQiOjE2NjIyODIwMjd9.lm70bh92Ruu6L0wsw41n-NbeRnPg5vZbKeUo1juCnA4" --region iran --detach